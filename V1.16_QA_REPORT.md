# BizControl Online V1.16 — Final QA Report

## Core functional flow

Working build V1.16 mencakup dan telah diuji pada alur fitur sebelumnya:
- buka shift dan modal kas awal;
- barcode scan + qty;
- CRM member di POS;
- promo persen/nominal, service, pajak, redeem point;
- checkout, pengurangan stok dan point;
- invoice popup + A4/Thermal;
- refund sebagian dan pengembalian stok/point;
- stock opname;
- recipe/BOM mengurangi bahan baku;
- tutup shift dan expected cash.

## Final role/security regression

**41/41 PASS** pada static role/data-scope audit.

Perbaikan final:
- Kasir: `sales_edit` dihapus.
- Finance: menu POS disembunyikan.
- Gudang: sales menjadi fulfilment/Surat Jalan tanpa harga/payment/HPP; PO dimasking secara server-side.
- Staff: dashboard non-finansial + produk read-only.
- Product Opname/Recipe permission-gated.
- CRM mutation Kasir dibatasi kolom aman; point/total_spend/visit_count bukan direct editable fields.
- Warehouse product DB guard diperluas untuk barcode/variant/inventory_mode.
- PO direct SELECT finansial dibatasi Owner/Admin/Finance; Gudang memakai masked RPC.
- RLS V1.10–V1.16 dan fixed `search_path` SECURITY DEFINER diperiksa.
- Migration tidak melakukan DROP TABLE / TRUNCATE.

## Syntax / package checks

- `app.js`: PASS.
- `v116.js`: PASS.
- `demo-sales-config.js`: PASS.
- `demo-sales-layer.js`: PASS.
- Runtime dependency presence: PASS.
- ZIP integrity: lakukan `unzip -t` pada paket final (hasil dicatat saat packaging).

## Batas QA environment

Browser automation final tidak dapat dijalankan karena navigation diblokir policy runtime (`ERR_BLOCKED_BY_ADMINISTRATOR`). Karena itu tidak ada klaim screenshot role baru pada final hardening. Lakukan staging smoke test per role sebelum deploy production.

## Supabase target

Connected Supabase tool pada sesi packaging menunjuk project yang berbeda dari Project URL di `runtime-config.js`, sehingga migration **tidak dijalankan otomatis** agar tidak menyentuh project yang salah. Jalankan migration pada project BizControl yang benar setelah backup.
