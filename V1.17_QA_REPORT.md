# BizControl Online V1.17 — QA Report

## Static checks
**46/46 V1.17 static production-readiness checks PASS.**

- `app.js`: JS syntax PASS.
- `v116.js`: JS syntax PASS.
- `v117.js`: JS syntax PASS.
- `sw.js`: JS syntax PASS.
- `account-admin-edge-function.ts`: TypeScript parse/transpile PASS.
- Runtime references V1.17 tersedia.
- Migration tidak memiliki `DROP TABLE` atau `TRUNCATE` statement.
- Subscription, onboarding, telemetry dan backup tables memakai RLS.
- Sensitive SECURITY DEFINER helpers menggunakan fixed `search_path`.
- Browser EXECUTE helper internal direvoke; hanya RPC client yang diperlukan digrant.
- Service-role key tetap hanya dipakai pada Edge Function server.

## Subscription/security checks
- Expired/Suspended menghasilkan `can_write=false`.
- DB write guard dipasang pada tabel bisnis sensitif.
- Backup/event tables sengaja tidak terkena expiry write guard agar customer expired masih bisa membuat bukti backup/telemetry.
- Admin Sistem subscription action memerlukan email yang ada di `BIZCONTROL_SYSTEM_ADMIN_EMAILS`.
- Owner tidak dapat menulis langsung ke `business_subscriptions` melalui RLS.
- Cloud V1.17 membatasi bisnis baru kedua per Owner.

## Browser QA limitation
Environment Chromium yang tersedia untuk QA memblokir navigasi localhost dengan enterprise policy: `Your organization doesn’t allow you to view this site`. Karena itu visual/browser smoke test V1.17 final tidak dapat diklaim dari environment ini.

Wajib lakukan smoke test pada staging asli setelah migration dan deploy, mengikuti `00_README_V1.17.md`.

## Release gate
Static QA bukan pengganti staging smoke test. Production release tetap mensyaratkan validasi migration pada project Supabase BizControl yang benar, Edge Function redeploy, role smoke test, subscription Active/Expired test, offline test, backup verification, dan hardware print/barcode test yang relevan.
