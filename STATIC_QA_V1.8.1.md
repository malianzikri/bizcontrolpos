# BizControl Online V1.8.1 — Static QA

**Result: 54/54 PASS**

Checks cover JavaScript syntax, versioning, Forgot/Reset Password flow, generic recovery message, password minimum, runtime config, sessionStorage, CSP/header configuration, public-schema CREATE revoke, default privilege revocation, explicit RPC EXECUTE grants, HPP/gross-profit masking in mutation/list RPCs, Warehouse financial masking, legacy RPC revocation, XSS escaping helper, mobile responsive CSS, Security Key lockout, atomic sale RPC, and atomic payment RPC.

`node --check app.js`: **PASS**.
